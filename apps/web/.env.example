# Origin of the Python server. The web app never talks to Google directly: it
# reads the login from the server's session cookie, so no client id lives here.
NUXT_PUBLIC_SERVER_API_BASE=http://localhost:8000
